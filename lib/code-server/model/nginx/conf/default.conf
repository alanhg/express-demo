events { }

http {

server {
    listen       36000;
    server_name  localhost;

    add_header X-Frame-Options "ALLOW-FROM=http://127.0.0.1:8000";

    location / {
        proxy_pass http://127.0.0.1:35999;
    }
}
}
