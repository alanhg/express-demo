<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Open AI测试</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
          integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script src="/js/utils.js"></script>
</head>
<body>
<main class="container">
    <h1>输入命令描述，生成命令</h1>
    <div class="mb-3">
        <label for="formGroupExampleInput" class="form-label">Prompt</label>
        <textarea class="form-control" id="aiInput">curl请求https://1991421.cn，获取网页状态码</textarea>
    </div>
    <div class="mb-3">
        <label for="model" class="form-label">Model</label>
        <select class="form-control" id="model">
            <option value="gpt">
                GPT-3 模型(比如iTerm2)
            </option>
            <option value="codex">
                Codex 模型(比如Fig]
            </option>
        </select>
    </div>
    <button class="btn btn-primary" onclick="goAi(event)">Go</button>
    <div class="highlight">
    <pre>
         <code id="ai-output"></code>
     </pre>
    </div>
</main>

</body>
<script>
  function goAi(e) {
    e.target.innerText = 'loading';
    goOpenAi({
      model: document.getElementById('model').value.trim(),
      prompt: document.getElementById('aiInput').value
    }).then(res => {
      document.getElementById('ai-output').innerHTML = res.answer;
      e.target.innerText = 'Go';
    })
  }
</script>
</html>